# Environment Configuration Template
#
# SETUP INSTRUCTIONS:
# 1. For local development: cp .env.dev .env
# 2. For production: Use AWS Secrets Manager (DO NOT use .env files)
# 3. Fill in your specific values
# 4. NEVER commit .env files with real credentials
#
# Available environment templates:
#   - .env.dev  : Development environment
#   - .env.prod : Production environment (template only, use Secrets Manager)
#
# Configuration files (define resource names, settings per environment):
#   - config/common.yaml : Shared settings
#   - config/dev.yaml    : Development overrides
#   - config/prod.yaml   : Production overrides

# Environment
ENV=development  # Options: development, production

# AWS Configuration
AWS_REGION=us-east-1
AWS_ACCOUNT_ID=
AWS_PROFILE=nba-cap-dev

# For local AWS services (LocalStack)
# AWS_ENDPOINT_URL=http://localhost:4566

# Database (RDS PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=nba_cap_dev
DB_USERNAME=nba_cap_user
DB_PASSWORD=

# S3 Buckets (environment-specific naming)
S3_RAW_DATA_BUCKET=nba-cap-raw-data-dev
S3_PROCESSED_DATA_BUCKET=nba-cap-processed-data-dev
S3_MODEL_ARTIFACTS_BUCKET=nba-cap-model-artifacts-dev

# API Configuration
API_BASE_URL=http://localhost:8000
API_KEY=

# Frontend
FRONTEND_URL=http://localhost:3000
AMPLIFY_APP_ID=

# External Data Sources (optional)
NBA_API_KEY=
BASKETBALL_REF_API_KEY=

# Logging
LOG_LEVEL=DEBUG  # Options: DEBUG, INFO, WARNING, ERROR

# SageMaker Configuration
SAGEMAKER_EXECUTION_ROLE_ARN=
SAGEMAKER_ENDPOINT_NAME=nba-cap-endpoint-dev

# Production Notes:
# - Never use .env files in production
# - Store secrets in AWS Secrets Manager
# - Use IAM roles instead of access keys
# - Retrieve secrets at runtime using boto3
