# Environment Configuration Template
#
# SETUP INSTRUCTIONS:
# 1. For local development: cp .env.example .env
# 2. Fill in your specific values
# 3. NEVER commit .env files with real credentials
#
# Available environment templates:
#   - .env.dev  : Development environment
#   - .env.prod : Production environment (use AWS Secrets Manager)
#
# Phase 1 (MVP): ETL Pipeline Only
# - No API Gateway, Frontend, or SageMaker yet
# - Focus: Data gathering → Storage (RDS + S3)

# Environment
ENV=development  # Options: development, production

# AWS Configuration
AWS_REGION=us-east-1
AWS_ACCOUNT_ID=
AWS_PROFILE=default

# For local development with LocalStack (optional)
# AWS_ENDPOINT_URL=http://localhost:4566

# Database (RDS PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=nba_cap_optimizer
DB_USERNAME=admin
DB_PASSWORD=

# AWS Secrets Manager (for production)
# SECRET_ARN=arn:aws:secretsmanager:us-east-1:ACCOUNT:secret:nba-cap-ENV-db-credentials

# S3 Bucket (single bucket for raw/processed data)
DATA_BUCKET=nba-cap-development-data

# Step Functions (ETL Pipeline)
ETL_PIPELINE_ARN=

# External Data Sources (for fetching NBA stats)
NBA_API_KEY=  # Optional - some endpoints work without
BASKETBALL_REF_API_KEY=  # Optional

# Logging
LOG_LEVEL=DEBUG  # Options: DEBUG, INFO, WARNING, ERROR

# Notes:
# - Phase 2 will add: SageMaker for ML
# - Phase 3 will add: API Gateway for REST API
# - Phase 4 will add: Frontend (S3 + CloudFront)
